<Window x:Class="BiometricStudentPickup.AttendanceReportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:BiometricStudentPickup"
        Title="Attendance Report" Height="600" Width="800"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button x:Name="TodayButton" Content="Today" Width="80" Margin="0,0,10,0" Click="TodayButton_Click"/>
            <TextBlock Text="Select Date:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <DatePicker x:Name="DatePicker" Width="120" SelectedDateChanged="DatePicker_SelectedDateChanged"/>
            <Button x:Name="ExportButton" Content="Export CSV" Width="100" Margin="10,0,0,0" Click="ExportButton_Click"/>
        </StackPanel>

        <TextBlock x:Name="DateLabel" Grid.Row="1" FontSize="16" FontWeight="Bold" Margin="0,0,0,5"/>
        <TextBlock x:Name="SummaryLabel" Grid.Row="2" FontSize="14" Margin="0,0,0,10"/>

        <DataGrid x:Name="AttendanceDataGrid" Grid.Row="3" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Student Name" Binding="{Binding StudentName}" Width="200"/>
                <DataGridTextColumn Header="Class" Binding="{Binding ClassName}" Width="100"/>
                <DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="100"/>
                <DataGridTextColumn Header="Time In" Binding="{Binding TimeIn}" Width="100"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="80">
                    <DataGridTextColumn.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Foreground">
                                <Setter.Value>
                                    <Binding Path="Status">
                                        <Binding.Converter>
                                            <local:StatusToColorConverter/>
                                        </Binding.Converter>
                                    </Binding>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </DataGridTextColumn.CellStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="4" Content="Close" Width="80" HorizontalAlignment="Right" 
                Margin="0,10,0,0" Click="CloseButton_Click"/>
    </Grid>
</Window>