<Window x:Class="BiometricStudentPickup.AttendanceReportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:converters="clr-namespace:BiometricStudentPickup.Models"
        Title="📊 Attendance Report"
        Height="700"
        Width="1000"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="Segoe UI">

    <Window.Resources>
        <!-- Converter for status colors -->
        <converters:StatusToColorConverter x:Key="StatusToColorConverter"/>

        <!-- Custom Styles -->
        <Style x:Key="PrimaryButtonStyle"
               TargetType="Button">
            <Setter Property="materialDesign:ButtonAssist.CornerRadius"
                    Value="6"/>
            <Setter Property="Height"
                    Value="36"/>
            <Setter Property="FontSize"
                    Value="13"/>
            <Setter Property="FontWeight"
                    Value="SemiBold"/>
            <Setter Property="Cursor"
                    Value="Hand"/>
            <Setter Property="Padding"
                    Value="16,6"/>
            <Setter Property="HorizontalContentAlignment"
                    Value="Center"/>
        </Style>

        <Style x:Key="DateButtonStyle"
               BasedOn="{StaticResource PrimaryButtonStyle}"
               TargetType="Button">
            <Setter Property="Background"
                    Value="{DynamicResource PrimaryHueLightBrush}"/>
            <Setter Property="Foreground"
                    Value="{DynamicResource PrimaryHueMidForegroundBrush}"/>
            <Setter Property="BorderThickness"
                    Value="1"/>
            <Setter Property="BorderBrush"
                    Value="{DynamicResource PrimaryHueMidBrush}"/>
        </Style>

        <Style x:Key="ExportButtonStyle"
               BasedOn="{StaticResource PrimaryButtonStyle}"
               TargetType="Button">
            <Setter Property="Background"
                    Value="#10B981"/>
            <Setter Property="Foreground"
                    Value="White"/>
        </Style>

        <Style x:Key="StatusBadgeStyle"
               TargetType="Border">
            <Setter Property="CornerRadius"
                    Value="4"/>
            <Setter Property="Padding"
                    Value="8,3"/>
            <Setter Property="HorizontalAlignment"
                    Value="Center"/>
        </Style>

        <Style x:Key="DataGridHeaderStyle"
               TargetType="DataGridColumnHeader">
            <Setter Property="Background"
                    Value="{DynamicResource PrimaryHueLightBrush}"/>
            <Setter Property="Foreground"
                    Value="{DynamicResource PrimaryHueMidForegroundBrush}"/>
            <Setter Property="FontWeight"
                    Value="SemiBold"/>
            <Setter Property="Padding"
                    Value="12,8"/>
            <Setter Property="BorderThickness"
                    Value="0,0,0,2"/>
            <Setter Property="BorderBrush"
                    Value="{DynamicResource PrimaryHueMidBrush}"/>
        </Style>

        <Style x:Key="DataGridRowStyle"
               TargetType="DataGridRow">
            <Setter Property="BorderThickness"
                    Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver"
                         Value="True">
                    <Setter Property="Background"
                            Value="{DynamicResource PrimaryHueLightBrush}"/>
                </Trigger>
                <Trigger Property="IsSelected"
                         Value="True">
                    <Setter Property="Background"
                            Value="{DynamicResource PrimaryHueMidBrush}"/>
                    <Setter Property="Foreground"
                            Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header with Left Title and Right Summary -->
        <materialDesign:Card Grid.Row="0"
                             Padding="4"
                             Background="{DynamicResource PrimaryHueLightBrush}"
                             Margin="0,0,0,16">
            <DockPanel>
                <!-- Left Side: Title and Icon -->
                <StackPanel DockPanel.Dock="Left">
                    <StackPanel Orientation="Horizontal"
                                VerticalAlignment="Center">
                        <materialDesign:PackIcon Kind="ChartBar"
                                                 Width="32"
                                                 Height="32"
                                                 Foreground="{DynamicResource PrimaryHueMidBrush}"
                                                 Margin="0,0,16,0"/>
                        <StackPanel>
                            <TextBlock Text="ATTENDANCE REPORT"
                                       Foreground="{DynamicResource PrimaryHueMidForegroundBrush}"
                                       FontSize="22"
                                       FontWeight="Bold"/>
                            <TextBlock Text="Track student attendance records"
                                       Foreground="{DynamicResource MaterialDesignBody}"
                                       FontSize="14"
                                       Margin="0,4,0,0"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>

                <!-- Right Side: Summary Stats -->
                <Border DockPanel.Dock="Right"
                        Width="380"
                        Background="{DynamicResource MaterialDesignBackground}"
                        CornerRadius="8"
                        Padding="16,12"
                        Margin="185,0,0,0"
                        BorderThickness="1"
                        BorderBrush="{DynamicResource MaterialDesignDivider}">
                    <StackPanel>
                        <!-- Date -->
                        <TextBlock x:Name="DateLabel"
                                   Text="{Binding SelectedDate, StringFormat='dddd, MMMM d, yyyy'}"
                                   Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                                   Foreground="{DynamicResource PrimaryHueMidBrush}"
                                   Margin="0,0,0,8"
                                   HorizontalAlignment="Center"/>

                        <!-- Total Summary -->
                        <Border Background="{DynamicResource PrimaryHueLightBrush}"
                                CornerRadius="6"
                                Padding="12,8"
                                Margin="0,0,0,8">
                            <TextBlock x:Name="SummaryLabel"
                                       Text="24 students present out of 27 total"
                                       Style="{StaticResource MaterialDesignBody1TextBlock}"
                                       Foreground="{DynamicResource PrimaryHueMidForegroundBrush}"
                                       FontWeight="SemiBold"
                                       HorizontalAlignment="Center"/>
                        </Border>
                    </StackPanel>
                </Border>
            </DockPanel>
        </materialDesign:Card>

        <!-- Date and Filter Controls -->
        <materialDesign:Card Grid.Row="1"
                             Padding="4"
                             Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal"
                        VerticalAlignment="Center">
                <!-- Today Button -->
                <Button x:Name="TodayButton"
                        Content="TODAY"
                        Style="{StaticResource DateButtonStyle}"
                        Click="TodayButton_Click"
                        Margin="0,0,12,0">
                    <Button.ContentTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal"
                                        HorizontalAlignment="Center">
                                <materialDesign:PackIcon Kind="CalendarToday"
                                                         Width="16"
                                                         Height="16"
                                                         Margin="0,0,6,0"/>
                                <TextBlock Text="{Binding}"
                                           VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </Button.ContentTemplate>
                </Button>

                <!-- Date Picker -->
                <StackPanel Orientation="Horizontal"
                            VerticalAlignment="Center"
                            Margin="0,0,20,0">
                    <materialDesign:PackIcon Kind="Calendar"
                                             Width="20"
                                             Height="20"
                                             Foreground="{DynamicResource PrimaryHueMidBrush}"
                                             Margin="0,0,8,0"/>
                    <TextBlock Text="Select Date:"
                               Style="{StaticResource MaterialDesignBody1TextBlock}"
                               VerticalAlignment="Center"
                               Margin="0,0,8,0"/>
                    <DatePicker x:Name="DatePicker"
                                Width="140"
                                materialDesign:HintAssist.Hint="Choose date"
                                SelectedDateChanged="DatePicker_SelectedDateChanged"/>
                </StackPanel>

                <!-- Grade/Class Filter -->
                <StackPanel Orientation="Horizontal"
                            VerticalAlignment="Center"
                            Margin="0,0,20,0">
                    <materialDesign:PackIcon Kind="AccountSchool"
                                             Width="20"
                                             Height="20"
                                             Foreground="{DynamicResource PrimaryHueMidBrush}"
                                             Margin="0,0,8,0"/>
                    <TextBlock Text="Filter by Class:"
                               Style="{StaticResource MaterialDesignBody1TextBlock}"
                               VerticalAlignment="Center"
                               Margin="0,0,8,0"/>
                    <ComboBox x:Name="ClassFilterComboBox"
                              Width="140"
                              materialDesign:HintAssist.Hint="All Classes"
                              SelectionChanged="ClassFilterComboBox_SelectionChanged">
                        <ComboBoxItem Content="All Classes"
                                      IsSelected="True"/>
                        <!-- Items will be populated from code-behind -->
                    </ComboBox>
                </StackPanel>

                <!-- Export Button -->
                <Button x:Name="ExportButton"
                        Content="EXPORT CSV"
                        Style="{StaticResource ExportButtonStyle}"
                        Click="ExportButton_Click"
                        HorizontalAlignment="Right">
                    <Button.ContentTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal"
                                        HorizontalAlignment="Center">
                                <materialDesign:PackIcon Kind="Download"
                                                         Width="16"
                                                         Height="16"
                                                         Margin="0,0,6,0"/>
                                <TextBlock Text="{Binding}"
                                           VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </Button.ContentTemplate>
                </Button>
            </StackPanel>
        </materialDesign:Card>

        <!-- Attendance Data Grid -->
        <materialDesign:Card Grid.Row="3"
                             Padding="0"
                             Margin="0,0,0,2">
            <DataGrid x:Name="AttendanceDataGrid"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserReorderColumns="False"
                      CanUserSortColumns="True"
                      RowHeight="48"
                      GridLinesVisibility="None"
                      HeadersVisibility="Column"
                      SelectionMode="Single"
                      SelectionUnit="FullRow"
                      BorderThickness="0"
                      Background="Transparent">

                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader"
                           BasedOn="{StaticResource DataGridHeaderStyle}"/>
                    <Style TargetType="DataGridRow"
                           BasedOn="{StaticResource DataGridRowStyle}"/>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness"
                                Value="0"/>
                        <Setter Property="Padding"
                                Value="12,8"/>
                        <Setter Property="VerticalAlignment"
                                Value="Center"/>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <!-- Student Name -->
                    <DataGridTextColumn Header="👤 STUDENT NAME"
                                        Binding="{Binding StudentName}"
                                        Width="2.5*">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment"
                                        Value="Center"/>
                                <Setter Property="FontWeight"
                                        Value="Medium"/>
                                <Setter Property="Padding"
                                        Value="20"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <!-- Class -->
                    <DataGridTextColumn Header="🏫 CLASS"
                                        Binding="{Binding ClassName}"
                                        Width="1*">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment"
                                        Value="Center"/>
                                <Setter Property="HorizontalAlignment"
                                        Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <!-- Date -->
                    <DataGridTextColumn Header="📅 DATE"
                                        Binding="{Binding Date, StringFormat='dd-MMM-yyyy'}"
                                        Width="1.2*">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment"
                                        Value="Center"/>
                                <Setter Property="HorizontalAlignment"
                                        Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <!-- Time In -->
                    <DataGridTextColumn Header="⏰ TIME IN"
                                        Binding="{Binding TimeIn, StringFormat='hh\\:mm tt'}"
                                        Width="1.2*">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment"
                                        Value="Center"/>
                                <Setter Property="HorizontalAlignment"
                                        Value="Center"/>
                                <Setter Property="FontFamily"
                                        Value="Consolas"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <!-- Status -->
                    <DataGridTemplateColumn Header="🔰 STATUS"
                                            Width="1.5*"
                                            IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource StatusBadgeStyle}"
                                        Background="{Binding Status, Converter={StaticResource StatusToColorConverter}}">
                                    <TextBlock Text="{Binding Status}"
                                               Foreground="White"
                                               FontWeight="SemiBold"
                                               FontSize="11"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </materialDesign:Card>
        <!-- Simple Close Button Footer -->
        <Border Grid.Row="4"
                BorderThickness="0,0,0,0"
                BorderBrush="#E0E0E0">
            <StackPanel HorizontalAlignment="Right">
                <Button x:Name="CloseButton"
                        Content="Close"
                        Width="80"
                        Height="30"
                        Click="CloseButton_Click"
                        Background="#2196F3"
                        Foreground="White"
                        BorderThickness="2"
                        FontWeight="SemiBold"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>